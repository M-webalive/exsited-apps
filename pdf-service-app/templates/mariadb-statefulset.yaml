apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "pdf-service.fullname" . }}-mariadb
spec:
  serviceName: {{ include "pdf-service.fullname" . }}-mariadb
  replicas: 1
  selector:
    matchLabels:
      app: {{ include "pdf-service.fullname" . }}-mariadb
  template:
    metadata:
      labels:
        app: {{ include "pdf-service.fullname" . }}-mariadb
    spec:
      containers:
        - name: mariadb
          image: {{ .Values.mariadb.image }}
          env:
            - name: MYSQL_DATABASE
              value: {{ .Values.mariadb.database }}
            - name: MYSQL_ALLOW_EMPTY_PASSWORD
              value: "{{ .Values.mariadb.rootAllowEmptyPassword }}"
          ports:
            - containerPort: 3306
          volumeMounts:
            - name: mariadb-storage
              mountPath: /var/lib/mysql
  volumeClaimTemplates:
    - metadata:
        name: mariadb-storage
      spec:
        accessModes: [ "ReadWriteOnce" ]
        resources:
          requests:
            storage: {{ .Values.mariadb.persistence.size }}
        storageClassName: {{ .Values.mariadb.persistence.storageClass }}
